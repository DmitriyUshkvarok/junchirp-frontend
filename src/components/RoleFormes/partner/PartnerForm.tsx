import s from "./partnerForm.module.scss";

const PartnerForm = () => {
  return (
    <section className={s.section}>
      <div className={`${s.container}  `}>
        <h1>Partner Form</h1>
      </div>
    </section>
  );
};
export default PartnerForm;
