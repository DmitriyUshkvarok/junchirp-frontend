import s from "./investorForm.module.scss";

const InvestorForm = () => {
  return (
    <section className={s.section}>
      <div className={`${s.container}  `}>
        <h1>Investor Form</h1>
      </div>
    </section>
  );
};
export default InvestorForm;
