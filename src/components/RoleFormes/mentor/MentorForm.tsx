import s from "./mentorForm.module.scss";

const MentorForm = () => {
  return (
    <section className={s.section}>
      <div className={`${s.container}  `}>
        <h1>Mentor Form</h1>
      </div>
    </section>
  );
};
export default MentorForm;
